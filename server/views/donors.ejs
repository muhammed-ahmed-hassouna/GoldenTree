<!-- donors.ejs -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donors List</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        h1 {
            text-align: center;
        }

        .card-container {
            display: flex;
            flex-wrap: wrap;
            gap: 100px;
            justify-content: center;
            margin-top: 20px;
        }

        .card {
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            border-radius: 8px;
            transition: box-shadow 0.3s ease, transform 0.3s ease;
            width: 300px;
        }

        .card:hover {
            box-shadow: 0 8px 16px green;
            transform: scale(1.05);
        }

        h2 {
            color: #333;
        }

        p {
            color: #666;
        }
    </style>
</head>

<body>
    <!-- <h1>Donors List</h1> -->

    <% if (donors.length===0) { %>
        <p>No donors found.</p>
        <% } else { %>
            <div class="card-container">
                <% donors.forEach((donor)=> { %>
                    <div class="card">
                        <h2>
                            <%= donor.title %>
                        </h2>
                        <p>Donation Amount: <%= donor.DonationAmount %>
                        </p>
                        <p>Details: <%= donor.details %>
                        </p>
                        <p>Date: <%= donor.Date %>
                        </p>
                        <p>Image: <%= donor.image %>
                        </p>
                        <p>Is Completed: <%= donor.isCompleted %>
                        </p>
                        <p>Max Donation Amount: <%= donor.maxDonationAmount %>
                        </p>
                        <p>Current Donation Amount: <%= donor.currentDonationAmount %>
                        </p>
                        <p>Expiration Time: <%= donor.expirationTime %>
                        </p>
                    </div>
                    <% }); %>
            </div>
            <% } %>

                <!-- Add your JavaScript or include a JavaScript framework here -->
</body>

</html>